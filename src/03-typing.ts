(() => {
  let product_name = '1';
  let product_price = 17;
  let cart = [];

  product_name.toLowerCase(); // only returns string methods when you put . after variable name reference
  product_price.valueOf(); // only returns number methods when you put . after variable name reference
  cart.length; // only returns array methods when you put . after variable name reference

  const product_stock = 1000;
})();
